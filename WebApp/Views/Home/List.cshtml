@model IEnumerable<DomainModel.Student>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<ul>
    @foreach (var student in Model)
    {
        <li>@student.FirstName @student.LastName (id: @student.PersonID)</li>
    }
</ul>